let suma = function(x, y) {return (x + y);}
let resta = function(x, y) {return (x - y);}
let multiplica = function(x, y) {return (x * y);}
let divide = function(x, y) {return (x / y);}
let hacerUnPunto = function(x, y){return `(${x}, ${y})`;}